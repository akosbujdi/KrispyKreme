import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#ff6347', // Replace this with your desired color (e.g., Tomato Red)
        },
        secondary: {
            main: '#4caf50', // Optional: customize the secondary color
        },
    },
});

export default theme;